Complexity Analyzer Program

Overview

The Complexity Analyzer Program is made in Java the application is designed to analyze the complexity of given Java and Kotlin files,
by that I mean it counts the number of named functions(if, else, switch, for, while, do) after the program is done it will output the names
of the three most complex functions(functions with the biggest complexity score) of the given files.

